= simple_form_for([@category, @list]) do |f|
  = f.error_notification
  .form-inputs
    = f.input :name
    = f.input :description
    = f.association :category
  .form-actions
    = f.button :submit